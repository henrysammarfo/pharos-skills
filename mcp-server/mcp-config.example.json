{
  "mcpServers": {
    "pharos-skills": {
      "command": "node",
      "args": ["mcp-server/index.js"],
      "cwd": "ABSOLUTE_PATH_TO/pharos-skills",
      "env": {
        "DEPLOYMENTS_PATH": "deployments.example.json"
      }
    }
  },
  "_notes": {
    "wallet_writes": "Place funded wallet.json in repo root OR set PRIVATE_KEY in env for write MCP tools",
    "read_only": "Read tools work with deployments.example.json only — no wallet needed",
    "judge_tests": "npm run test:sdk && npm run test:mcp (add --wallet for full on-chain write suite)"
  }
}
